# API Response Evidence — PARTIALLY CAPTURED (as text, not screenshots)

Real JSON responses already captured in REVIEW_PACKET.md under
"Live MDU Verification" (fetch_provenance_contract and
validate_schema_compatibility against BHIV-DS-MARITIME-AIS-LIVE-001).

Still needed: screenshots (not just text) of responses from MASTERDB's
own endpoints (/validate, /certify, /packages/register, /tantra/*,
/discovery/packages) via Swagger or curl.
