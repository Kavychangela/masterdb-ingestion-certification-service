# Swagger Screenshots — NOT YET CAPTURED

Run `uvicorn main:app --reload`, open http://127.0.0.1:8000/docs, and
screenshot:
- Full endpoint list (collapsed view)
- One expanded MDU endpoint (/mdu/status or /mdu/schema/{dataset_id})
- One expanded TANTRA endpoint
- One expanded discovery endpoint
